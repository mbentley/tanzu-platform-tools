apiVersion: spaces.tanzu.vmware.com/v1alpha1
kind: Space
metadata:
  name: mbentley-space2
  namespace: default
spec:
  availabilityTargets:
    - name: mbentley-at2
      replicas: 1
  revisionHistoryLimit: 10
  template:
    spec:
      profiles:
        - name: mbentley-profile
          values:
            inline: null
      resources: {}
  updateStrategy:
    type: RollingUpdate
